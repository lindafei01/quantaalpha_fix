# =============================================================================
# QuantaAlpha 环境配置模板
#
# 使用方法：
#   1. cp configs/.env.example .env
#   2. 修改下面的路径和 API Key
#   3. ./run.sh "你的探索方向"
# =============================================================================

# =============================================================================
# 路径配置（核心：只需修改这两个路径即可适配新环境）
# =============================================================================

# Qlib 数据目录（解压 cn_data.zip 后的路径，需包含 calendars/features/instruments 子目录）
QLIB_DATA_DIR=<your_qlib_data_path>  # 例如: /data/qlib/cn_data

# 输出根目录（所有结果、缓存、日志都存放在此目录下）
DATA_RESULTS_DIR=<your_results_path>  # 例如: /data/quantaalpha/results

# =============================================================================
# Qlib 配置
# =============================================================================
QLIB_PROVIDER_URI=<your_qlib_data_path>
CONDA_ENV_NAME=quantaalpha

# =============================================================================
# LLM API 配置
# =============================================================================
OPENAI_API_KEY=<your_api_key>
OPENAI_BASE_URL=<your_base_url>  # 例如: https://dashscope.aliyuncs.com/compatible-mode/v1

# 模型配置
REASONING_MODEL=<your_model>      # 例如: deepseek-v3
CHAT_MODEL=<your_model>           # 例如: deepseek-v3

# Embedding 模型（用于 RAG，可选）
# EMBEDDING_MODEL=text-embedding-v4
# EMBEDDING_API_KEY=<your_embedding_key>
# EMBEDDING_BASE_URL=<your_embedding_url>

# =============================================================================
# 全局配置（通常不需要修改）
# =============================================================================
USE_LOCAL=True
MAX_RETRY=5
RETRY_WAIT_SECONDS=5
FACTOR_MINING_TIMEOUT=999999
CHAT_MAX_TOKENS=4000
CHAT_TEMPERATURE=0.7
